{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["${author}"],
  "license": "MIT",
  "icon": "${logo}",
  "contact": {
    "homepage": "${homeUrl}",
    "sources": "${sourceUrl}",
    "issues": "${issuesUrl}"
  },
  "environment": "*",
  "entrypoints": {},
  "depends": {
    "fabricloader": "*",
    "minecraft": ">=${minecraftVersion}"
  }
}
