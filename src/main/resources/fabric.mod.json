{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["Leawind"],
  "contact": {
    "homepage": "${homeUrl}",
    "sources": "${sourceUrl}"
  },
  "environment": "*",
  "entrypoints": {},
  "depends": {
    "fabricloader": "*",
    "minecraft": ">=${minecraftVersion}"
  }
}
